import { IssuerActionTypes, issuerState } from './../../types/redux.types';

const initialState: issuerState = {

}

export default (state = initialState, action: any) => {
    switch (action.type) {
        default:
            return state
    }
}
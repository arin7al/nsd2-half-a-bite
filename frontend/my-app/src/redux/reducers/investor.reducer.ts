import { InvestorActionTypes, investorState } from './../../types/redux.types';

const initialState: investorState = {

}

export default (state = initialState, action: any) => {
    switch (action.type) {
        default:
            return state
    }
}



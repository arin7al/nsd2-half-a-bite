import { OperatorActionTypes, operatorState } from './../../types/redux.types';

const initialState: operatorState = {

}

export default (state = initialState, action: any) => {
    switch (action.type) {
        default:
            return state
    }
}